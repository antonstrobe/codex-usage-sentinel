# Include once in nginx http context, before the virtual hosts.
limit_req_zone $binary_remote_addr zone=codex_usage_requests:1m rate=5r/s;
limit_conn_zone $binary_remote_addr zone=codex_usage_connections:1m;
